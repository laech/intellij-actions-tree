<idea-plugin>
    <id>com.gitlab.lae.intellij.actions.tree</id>
    <name>Actions Tree</name>
    <vendor url="https://gitlab.com/lae/intellij-actions-tree">Lae</vendor>

    <description><![CDATA[
    <p>
    Like the built-in IntelliJ Quick Lists, for defining a list of actions to show in a popup,
    an action in the list can be other action list to show when invoked.
    The main difference between this plugin and Quick Lists is that this plugin allows each action
    in the list to be assigned a keyboard shortcut that is local to the list,
    doesn't conflict with other global shortcuts.
    This allows simulation of Emacs like prefix keys (n-level deep), so you are not restricted to
    IntelliJ keymap's maximum of two key strokes.
    </p>
    See https://gitlab.com/lae/intellij-actions-tree for more details.
    ]]></description>

    <!-- http://www.jetbrains.org/intellij/sdk/docs/basics/getting_started/build_number_ranges.html -->
    <idea-version since-build="181"/>

    <!-- http://www.jetbrains.org/intellij/sdk/docs/basics/getting_started/plugin_compatibility.html -->
    <depends>com.intellij.modules.lang</depends>

    <extensions defaultExtensionNs="com.intellij">
        <applicationConfigurable
                displayName="Actions Tree"
                id="com.gitlab.lae.intellij.actions.tree"
                parentId="preferences.keymap"
                instance="com.gitlab.lae.intellij.actions.tree.AppConfigurable"/>
    </extensions>

    <application-components>
        <component>
            <implementation-class>
                com.gitlab.lae.intellij.actions.tree.AppComponent
            </implementation-class>
        </component>
    </application-components>

    <actions>
        <group text="Actions Tree"
               id="com.gitlab.lae.intellij.actions.tree.group"
               popup="true">

            <add-to-group group-id="ToolsMenu" anchor="last"/>

            <action id="com.gitlab.lae.intellij.actions.tree.reload"
                    class="com.gitlab.lae.intellij.actions.tree.ReloadAction"
                    text="Reload"
                    description="Reloads actions from configuration file"/>

            <action id="com.gitlab.lae.intellij.actions.tree.ExportIdeActions"
                    class="com.gitlab.lae.intellij.actions.tree.ExportIdeActions"
                    text="Export IDE Actions"
                    description="Exports IDE actions to a file containing ID to name mappings"/>
        </group>
    </actions>
</idea-plugin>
